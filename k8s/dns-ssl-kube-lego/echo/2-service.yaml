apiVersion: v1
kind: Service
metadata:
  name: echo-service
  namespace: echo
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: echo-backend
